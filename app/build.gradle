apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'hackerchai'
            keyPassword 'hackchaiyisheng'
            storeFile file('G:/Android/hackerchai.jks')
            storePassword 'hackchaiyisheng'
        }
    }
    compileSdkVersion 21
    buildToolsVersion "21.1.2"
    defaultConfig {
        applicationId "com.hackerchai.wiouth"
        minSdkVersion 14
        targetSdkVersion 19
        versionCode 1
        versionName "1.0"
        signingConfig signingConfigs.config
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:21.0.3'
}
